# 健身计划 1.1.0

## 安装升级

应用已由「炼炼」更名为「健身计划」。最低 Android 8.0，版本号 1.1.0（versionCode 2）。保留原包名 com.lyistr2.lianlian、签名和本地存储键，可直接覆盖此前提供的 1.0.0 测试版，不需要卸载。清除数据或卸载会删除本机记录。

原计划和进行中的训练不会强制改写。要使用新版计划生成，请进入「计划」→「定制我的计划」，检查目标、水平、时间和训练日后，点击「应用设置并更新计划」。此操作会替换计划中的自定义参数，历史记录和进行中的训练保留。

## 计划页

- 自选每周训练日、目标、运动水平、单次时间、自动/全身分化、轻量/标准/更多组数。
- 七天日程同时呈现训练日与休息日；展示动作主要肌群的周组数。
- 新手自动分化在每周不超过三练时使用全身训练；较少组数，并给出余力说明。
- 主要复合动作先于辅助动作。训练时长按组数、次数和实际组间休息估算；超时则减少组数或动作，而不是截断显示数字。
- 不使用不相关肌群动作凑数；恢复日同样遵守器械与伤病过滤。
- 动作卡片展示组数、次数/秒数、休息和强度说明；可单独修改组数、数量、休息秒数与默认负重。
- 展示每个动作最近三次已完成训练记录，方便对比重量与次数。

## 训练页

- 重量和数量预先填好，做完只需一次点击「完成本组」。重量优先沿用记住的值；初次外加负重默认 5 kg，无外加负重默认为 0。默认值只是记录初值，不是推荐训练重量；哑铃统一按双手合计。
- 重量支持 ±0.5 kg；次数支持 ±1；计时动作支持 ±5 秒。也保留手动输入精确值。
- 下一组沿用刚记录的数值。动作预设会保存在本机，下次继续使用；在计划中修改默认值会更新该动作的预设。
- 页面与休息弹层均可撤销最近一组。正在进行的训练不会被另一日的「开始」操作覆盖。
- 秒数动作按秒显示与统计，不把秒数当成重复次数或计入重量乘次数的训练容量。中途换动作时，已记录组保留原动作身份。

## 验证

通过原计划引擎 13 项检查、统计测试、新增 192 种目标/水平/时间/频率组合检查，以及手机浏览器的排期、参数编辑、一键记录、微调、撤销、恢复、完成训练、深浅主题与 360/393/430 宽度检查。旧版存储结构和秒数动作也已验证。APK 编译与 Android Lint 检查通过；静态检查为 0 错误，保留依赖版本、JavaScript 必需设置、备份配置的 3 条提示。未连接安卓真机，未声称已进行真机测试。

训练量与余力设计参考 ACSM 成人抗阻训练综述的一般原则，不构成个体医疗处方：
https://pmc.ncbi.nlm.nih.gov/articles/PMC12965823/

## 开发

构建方式见 ANDROID.md。新增测试：`node scripts/plan-v2-test.js`。App 仍离线运行，没有接入大模型或云端。测试包使用调试签名，正式上架需要开发者自己的发布签名。
