# 健身仪表盘更新

主页参考 shadcn/ui dashboard-01：深色侧栏、四项指标、面积趋势图、每周训练与历史记录表；支持浅色切换及手机布局。

- 时间范围：近 7、30、90 天，指标、趋势和记录同步。
- 趋势指标：估算卡路里、估算训练时长、训练容量。日期滑块支持触摸和键盘。
- 训练记录：按名称或日期搜索，每页 5 条。
- 点击「查看示例」预览模拟数据；示例数据不写入个人记录。
- 新完成训练保存开始时的体重快照、估算时长和热量，档案后续变更不会重算旧记录。
- 无估算字段的旧记录显示「未记录」，图表缺失日期断开并用虚线标记，不伪装成零消耗。

## 估算说明

此处为粗略总能耗，包含静息消耗，不是运动手表测量的活动热量。
公式：MET × 体重 kg × 估算分钟 ÷ 60。
力量训练采用 3.5 MET，有氧统一假设 5 MET，热身/拉伸假设 2.3 MET；实际强度可能不同。
力量时长按每次动作 3 秒加已完成组之间的计划休息估算，非力量环节使用计划分钟。仅统计用户标记完成的内容，不把页面打开或退出期间的等待当作运动。
参考：https://pacompendium.com/conditioning-exercise/

## 本地使用

直接打开 index.html，或沿用项目 Docker 配置运行。无新增运行依赖。
测试：node scripts/smoke-test.js 与 node scripts/metrics-test.js。
